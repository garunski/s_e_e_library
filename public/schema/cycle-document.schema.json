{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://see.local/schema/cycle-document",
  "title": "Cycle document",
  "type": "object",
  "required": ["id", "name", "host", "schema_version", "stores", "stages"],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1
    },
    "name": {
      "type": "string",
      "minLength": 1
    },
    "host": {
      "type": "string",
      "enum": ["orchestrator", "knowledge", "stories", "events"]
    },
    "schema_version": {
      "type": "integer",
      "minimum": 1
    },
    "stores": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "uniqueItems": true
    },
    "stages": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "key",
          "verb",
          "workflow_definition_id",
          "trigger",
          "reads",
          "writes"
        ],
        "additionalProperties": false,
        "properties": {
          "key": { "type": "string", "minLength": 1 },
          "verb": { "type": "string", "minLength": 1 },
          "workflow_definition_id": { "type": "string", "minLength": 1 },
          "trigger": {
            "type": "string",
            "enum": ["tick", "schedule", "event", "manual"]
          },
          "reads": {
            "type": "array",
            "items": { "type": "string", "minLength": 1 }
          },
          "writes": { "type": "string", "minLength": 1 }
        }
      }
    }
  }
}
